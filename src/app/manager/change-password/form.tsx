"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
export function ForcedPasswordForm() { const router = useRouter(); const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const values = new FormData(event.currentTarget); const response = await fetch("/api/auth/change-password", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ currentPassword: values.get("currentPassword"), newPassword: values.get("newPassword") }) }); const body = await response.json(); if (response.ok) router.push("/manager"); else setError(body.message ?? "Password change failed"); } return <form className="mt-6 grid gap-4" onSubmit={submit}><label className="grid gap-1 font-semibold text-slate-800">Current password<input className="rounded-lg border p-3" name="currentPassword" type="password" required /></label><label className="grid gap-1 font-semibold text-slate-800">New password<input className="rounded-lg border p-3" name="newPassword" type="password" minLength={8} required /></label>{error && <p className="rounded-lg bg-red-50 p-3 text-red-800" role="alert">{error}</p>}<button className="rounded-lg bg-emerald-700 p-3 font-bold text-white" type="submit">Save password</button></form>; }
