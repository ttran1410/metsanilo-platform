"use client";
import { useState, type FormEvent } from "react";
import { AdminNotice, AdminPageHeader } from "./presentation";
type Product = { product: { id: string; nameFi: string }; packages: Array<{ id: string; labelFi: string; volumeMl: number; priceCents: number }> };
export function ManualOrdersModule({ products }: { products: Product[] }) { const [historical, setHistorical] = useState(false); const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const values = new FormData(event.currentTarget); const product = products.find((row) => row.product.id === values.get("productId")); const packageRow = product?.packages.find((row) => row.id === values.get("packageId")); const quantity = Number(values.get("quantity")); const paymentEuros = Number(values.get("paymentEuros")); const common = { productId: values.get("productId"), packageId: values.get("packageId"), quantity, fulfillmentDate: values.get("fulfillmentDate"), fulfillmentMethod: values.get("fulfillmentMethod"), customerName: values.get("customerName"), mobile: values.get("mobile"), email: values.get("email") || undefined, streetAddress: values.get("streetAddress") || undefined, postalCode: values.get("postalCode") || undefined, city: values.get("city") || undefined, source: String(values.get("source") ?? "PHONE"), ...(historical ? { completedStatus: values.get("completedStatus"), completedAt: new Date().toISOString(), reason: values.get("reason"), itemSubtotalCents: packageRow ? packageRow.priceCents * quantity : undefined, paymentAmountCents: paymentEuros > 0 ? Math.round(paymentEuros * 100) : undefined } : { status: "NEW" }) }; const response = await fetch(historical ? "/api/admin/orders/historical" : "/api/admin/orders/external", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(common) }); const body = await response.json(); if (!response.ok) return setMessage(body.message ?? body.code ?? "Request failed"); setMessage(historical ? "Completed order recorded." : "Manual order created."); event.currentTarget.reset(); }
  return <section className="shell pb-10"><AdminPageHeader eyebrow="ORDER INTAKE" title="Create order" description="Create a manual customer order or record a completed historical order." actions={<label className="flex items-center gap-2"><input type="checkbox" checked={historical} onChange={(event) => setHistorical(event.target.checked)} /> Record historical order</label>} />{message && <AdminNotice tone="success" live>{message}</AdminNotice>}<form className="card mt-3 grid gap-3 md:grid-cols-2" onSubmit={submit}><label className="field"><span>Product</span><select name="productId" required>{products.map((row) => <option key={row.product.id} value={row.product.id}>{row.product.nameFi}</option>)}</select></label><label className="field"><span>Package &amp; price</span><select name="packageId" required>{products.flatMap((row) => row.packages.map((item) => <option key={item.id} value={item.id}>{row.product.nameFi} · {item.labelFi} · {(item.priceCents / 100).toFixed(2)} €</option>))}</select></label><label className="field"><span>Quantity</span><input name="quantity" type="number" min="1" defaultValue="1" required /><small>Price is calculated from the package and can be adjusted after creation.</small></label><label className="field"><span>Fulfillment date</span><input name="fulfillmentDate" type="date" required /></label><label className="field"><span>Customer name</span><input name="customerName" required /></label><label className="field"><span>Mobile</span><input name="mobile" required /></label><label className="field"><span>Email</span><input name="email" type="email" /></label><label className="field"><span>Source</span><select name="source"><option value="PHONE">Phone</option><option value="SMS">SMS</option><option value="WHATSAPP">WhatsApp</option><option value="FACEBOOK">Facebook Message</option></select></label><label className="field"><span>Fulfillment method</span><select name="fulfillmentMethod"><option value="PICKUP">Pickup</option><option value="DELIVERY">Delivery</option></select></label><label className="field md:col-span-2"><span>Customer address</span><input name="streetAddress" placeholder="Street address, postal code, city" /></label>{historical && <><label className="field"><span>Completed status</span><select name="completedStatus"><option value="PICKED_UP">Picked up</option><option value="DELIVERED">Delivered</option></select></label><label className="field"><span>Payment received (€)</span><input name="paymentEuros" type="number" min="0" step="0.01" placeholder="e.g. 47.00" /></label><label className="field"><span>Reason / evidence note</span><input name="reason" required minLength={2} /></label></>}<button className="btn w-fit" type="submit">{historical ? "Record historical order" : "Create manual order"}</button></form></section>; }
