"use client";
import { useState, type FormEvent } from "react";
type Product = { product: { id: string; nameFi: string }; packages: Array<{ id: string; labelFi: string; volumeMl: number }> };
export function ManualOrdersModule({ products }: { products: Product[] }) { const [historical, setHistorical] = useState(false); const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const values = new FormData(event.currentTarget); const product = products.find((row) => row.product.id === values.get("productId")); const packageRow = product?.packages.find((row) => row.id === values.get("packageId")); const quantity = Number(values.get("quantity")); const common = { productId: values.get("productId"), packageId: values.get("packageId"), quantity, fulfillmentDate: values.get("fulfillmentDate"), fulfillmentMethod: values.get("fulfillmentMethod"), customerName: values.get("customerName"), mobile: values.get("mobile"), email: values.get("email") || undefined, streetAddress: values.get("streetAddress") || undefined, postalCode: values.get("postalCode") || undefined, city: values.get("city") || undefined, source: "PHONE", ...(historical ? { completedStatus: values.get("completedStatus"), completedAt: new Date().toISOString(), reason: values.get("reason"), itemSubtotalCents: packageRow ? packageRow.volumeMl * 0 : undefined } : { status: "NEW" }) }; const response = await fetch(historical ? "/api/admin/orders/historical" : "/api/admin/orders/external", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(common) }); const body = await response.json(); if (!response.ok) return setMessage(body.message ?? body.code ?? "Request failed"); setMessage(historical ? "Completed order recorded." : "Manual order created."); event.currentTarget.reset(); }
  return <section className="shell pb-10"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-bold">Manual orders</h2><p className="text-slate-600">Create a phone order or record a completed order.</p></div><label className="flex items-center gap-2"><input type="checkbox" checked={historical} onChange={(event) => setHistorical(event.target.checked)} /> Record completed order</label></div>{message && <p className="card mt-3" role="status">{message}</p>}<form className="card mt-3 grid gap-3 md:grid-cols-2" onSubmit={submit}><label className="field"><span>Product</span><select name="productId" required>{products.map((row) => <option key={row.product.id} value={row.product.id}>{row.product.nameFi}</option>)}</select></label><label className="field"><span>Package</span><select name="packageId" required>{products.flatMap((row) => row.packages.map((item) => <option key={item.id} value={item.id}>{row.product.nameFi} · {item.labelFi}</option>))}</select></label><label className="field"><span>Quantity</span><input name="quantity" type="number" min="1" defaultValue="1" required /></label><label className="field"><span>Fulfillment date</span><input name="fulfillmentDate" type="date" required /></label><label className="field"><span>Customer name</span><input name="customerName" required /></label><label className="field"><span>Mobile</span><input name="mobile" required /></label><label className="field"><span>Email (optional)</span><input name="email" type="email" /></label><label className="field"><span>Method</span><select name="fulfillmentMethod"><option value="PICKUP">Pickup</option><option value="DELIVERY">Delivery to be agreed</option></select></label><label className="field md:col-span-2"><span>Address (for delivery)</span><input name="streetAddress" /></label>{historical && <><label className="field"><span>Completed status</span><select name="completedStatus"><option value="PICKED_UP">Picked up</option><option value="DELIVERED">Delivered</option></select></label><label className="field"><span>Reason / evidence note</span><input name="reason" required minLength={2} /></label></>}<button className="btn w-fit" type="submit">{historical ? "Record completed order" : "Create manual order"}</button></form></section>; }
